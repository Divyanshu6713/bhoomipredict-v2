# Offline model pipeline (ml/train.py).
# shap is optional: without it the pipeline still runs and case-level
# explanations fall back to the linear surrogate, which the UI states.
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
shap>=0.44
